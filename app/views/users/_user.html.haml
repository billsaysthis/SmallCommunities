.user.list_row{:class => cycle('list_odd', 'list_even'), :id => ['user', user.id]}
	.list_label= user.name + (user.admin? ? ' (A)' : '')
	.list_detail= link_to(user.label, user.url) + " " + link_to_twitter(user)